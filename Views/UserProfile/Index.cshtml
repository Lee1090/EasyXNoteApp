
@{
    ViewBag.Title = "UserList";
}
<style>
    main {
        margin-top: 20px; /* 设置 main 元素顶部的外边距 */
    }

    h2 {
        font-size: 1.2em;
        margin-bottom: 10px; /* 设置标题底部的外边距 */
    }

    table {
        width: 100%;
        border-collapse: collapse;
        margin-top: 10px;
    }

    th, td {
        padding: 10px;
        text-align: left;
        border-bottom: 1px solid #ddd;
    }

    th {
        background-color: #f2f2f2;
    }
</style>

<main aria-labelledby="title">
    <h2 id="title"  style="font-size: 1.2em;">@ViewBag.Title</h2>
    <!--<h3>@ViewBag.Message</h3>-->
    @if (Model != null)
    {
        <table>
            <thead>
                <tr>
                    <th>User ID</th>
                    <th>User Name</th>
                    <th>Email</th>
                    <th>Created Date</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var userProfile in Model)
                {
                    <tr>
                        <td>@userProfile.UserID</td>
                        <td>@userProfile.UserName</td>
                        <td>@userProfile.Email</td>
                        <td>@userProfile.CreatedDate.ToString("yyyy-MM-dd HH:mm:ss")</td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p>No user data available.</p>
        <h3>@ViewBag.ErrorMessage</h3>
    }
</main>